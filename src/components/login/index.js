import { useNavigate } from "react-router-dom";
import { Background, Form } from "./style.js";

const TopInfo = () => {
  const navigate = useNavigate();

  return (
    <Background>
      <Form>
        <></>
      </Form>
    </Background>
  );
};

export default TopInfo;
